<p id="notice"><%= notice %></p>
<div class="container">
<h1 class="text-center">Products</h1><br>
<table class="table table-hover border">
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Price</th>
      <th>Image</th>
      <th>Category</th>
      <th colspan="3"></th>
    </tr>
  </thead>
 
  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.description %></td>
        <td><%= product.price %></td>
        <td><a href="/product/<%= product.id %>"><%= image_tag product.image.url(:thumb) %></a></td>
        <td><%= product.category.name %></td>
        <td><%= link_to 'Show', product,:class => 'btn blue-gradient waves-effect waves-light' %></td>
        <td><%= link_to 'Edit', edit_product_path(product),:class => 'btn peach-gradient waves-effect waves-light' %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-danger waves-effect waves-light' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

 <center class="digg_pagination">
  <%= will_paginate @products, :renderer=> BootstrapPagination::Rails, previous_label: "Older", next_label: "Newer", inner_window: 1, outer_window: 0 %>
</center>
<br>
</div>

<center>
  <%= link_to 'New Product', new_product_path, :class => 'btn btn--blue btn--stroke btn--stroke--2 px24 py12 txt-s round-full px36' %>
</center>
<style type="text/css">

element.style {
}
.btn.btn--stroke {
    background: linear-gradient(40deg,#45cafc,#303f9f) !important;
    transition: .5s ease;
    color: #fff;
}

.btn--stroke.btn--blue {
    color: #4264fb;
}
.btn.btn--stroke {
    background: linear-gradient(40deg,#45cafc,#303f9f) !important;
    transition: .5s ease;
    color: #fff;
}
.py12 {
    padding-top: 12px!important;
    padding-bottom: 12px!important;
}

.round-full {
    border-radius: 9999px!important;
}
.txt-s {
    font-size: 12px;
    line-height: 18px;
}
.btn--stroke--2 {
    box-shadow: inset 0 0 0 2px currentColor;
}

.px36 {
    padding-left: 36px!important;
    padding-right: 36px!important;
}
</style>
